import Header from "./components/Header";

function TodoList() {
  const addUndoItem = () => {};

  return (
    <div>
      <Header addUndoItem={addUndoItem} />
    </div>
  );
}

export default TodoList;
